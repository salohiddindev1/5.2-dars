let ism = prompt("Ismingizni kiriting:");
let spaceId = prompt("Space ID ni kiriting:");
let spacePassword = prompt("Space parolingizni kiriting:");

let object = {
  ism: ism,
  spaceId: spaceId,
  spacePassword: spacePassword,
};

console.log(object);
